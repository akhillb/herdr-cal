{
  "name": "herdr-attention",
  "version": "0.2.0",
  "private": true,
  "description": "herdr plugin: a unified attention feed that merges sources (meetings, and more via addons) into NOW / SOON / WATCHING tiers with triage actions.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "demo": "ATTENTION_DEMO=1 node src/board.js"
  }
}
